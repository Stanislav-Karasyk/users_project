import styles from './UserInfoPage.module.scss';

function UserInfoPage() {
  return (
    <p className={styles.message}>"Select a user to see their information"</p>
  );
}

export default UserInfoPage;
